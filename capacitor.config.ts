import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Sistema de Control de Asistencia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
